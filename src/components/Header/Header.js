import React from 'react'

const Header = () => {
    return (
        <nav className="navbar navbar-dark bg-dark bg-primary">
            <div className="container-fluid">
                <span className="navbar-brand mb-0 h1">Telecom Weather</span>
            </div>
        </nav>
    )
}

export default Header
